/** @format */

export function mockTranslate(source: string, targetLang: string): string {
  // For demo purposes, just return a fake adapted string
  return `[${targetLang}] ${source} (adapted)`;
}
